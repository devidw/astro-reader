p
ul
ol
pre
table
img
  @apply mb-4

li + li
li > ul
li > ol
  @apply mt-3

h2
h3
h4
h5
h6
  @apply text-(3xl gray-5) mb-4 font-sans

  &:not(:first-child)
    @apply mt-12

h1
  @apply text-(5xl) leading-tight capitalize

hr
  @apply border-dark-1 my-8

li
  list-style-position inside
  text-indent -1.5em
  margin-left 1.5em

  &::before
    content '—'
    margin-right 0.5em
    @apply text-gray-5 font-bold

  p
    @apply inline

blockquote
  @apply text-gray-5 italic border-l-1 border-dark-1 pl-4

a
  @apply text-sky-2

:not(pre) code
  @apply bg-dark-1 rounded-1 px-1

pre
  @apply rounded-2 p-4 hyphens-none text-sm border-(1 dark-1) overflow-x-auto h-full
  background #000 !important

img
  @apply rounded-2 select-none drag-none

  @media (min-width 768px)
    max-width none !important

table
  @apply w-full border-collapse border border-dark-1

  th
  td
    @apply border border-dark-1 p-2
  
  p
    @apply inline

h1
h2
h3
h4
h5
h6
pre
img
  @media (min-width 768px)
    width 120%
    margin-left -10%